GO_LOCALCMS_DBLAYER_V3_ENV_GLOBAL_WIRE_01
=========================================
DATE    : 2026-05-05
VERDICT : PASS

LIVRAISONS
----------
localcms-v5.html  — MOD_ENV_GLOBAL bridgé au M3 Config Store

COMPORTEMENT LIVRÉ
------------------
init()
  render() synchrone (defaults)
  GET /api/config/env_global (async)
  si 200 → _buf = data.data, _applyBuf() hydrate les champs visibles
  si 404 / erreur → silencieux, defaults conservés

save()
  _collectGroup() → fusionne DOM → _buf
  POST /api/config/env_global { data: _buf }
  OK   → BUS.emit('log:add','OK','Profil ENV Global sauvegardé')
  FAIL → BUS.emit('log:add','FAIL', detail ou message)

setGroup(g)
  _collectGroup() avant switch
  render() nouveau groupe
  _applyBuf() réapplique _buf

_buf
  accumulateur inter-groupes, peuplé par API au load + DOM à la saisie

CI PASS
-------
integration_test_pipeline.py          26/26
integration_test_shared_explorer.py   26/26
integration_test_config_store.py       8/8
npm run test:adopt                   540/540
shared-explorer.smoke.js (live)        6/6
cms-installer.smoke.mjs (live)        10/10
cms-config.smoke.mjs (live)            5/5

COMMIT
------
88a3cad  feat: wire env-global to M3 config store

ÉTAT GIT
--------
Branche : main
HEAD    : 88a3cad
Sync    : main...origin/main synchronisé

PROCHAIN GO
-----------
GO_LOCALCMS_DBLAYER_V3_RELEASE_PREP_01
  - Bumper main.py version 1.0.2 → 1.0.3
  - CI PASS final
  - Préparer tag v0.3.0-dblayer

POINT DE REPRISE
----------------
cd "$HOME/localcms"
git status --short --branch
git log --oneline -8
bash scripts/run-ci-local.sh
